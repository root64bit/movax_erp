# Deployment Report: DEPLOYMENT-2026-07-28-01

> **Deployment ID:** DEPLOYMENT-2026-07-28-01  
> **Timestamp:** 2026-07-28 14:35:09 UTC  
> **Target Project Ref:** `bkbcgndzsfylwsinxwbb`  
> **Target Host:** `aws-0-eu-west-1.pooler.supabase.com:6543`  
> **Executor:** Senior Implementation Architect  
> **Git Commit:** [`95b7aef`](https://github.com/root64bit/casadepeneus/commit/95b7aef)  
> **Pre-Deployment Backup:** Baseline Snapshot `05-pre-deployment-backup-report.md`  

---

## 1. Migrations Applied

| Migration File | Hash / Status | Execution Time | Result |
|----------------|---------------|----------------|--------|
| `20260728162000_001_core_schemas_and_company_config.sql` | Applied | < 1.2s | **SUCCESS** |

---

## 2. Objects Deployed to Production (`bkbcgndzsfylwsinxwbb`)

### Schemas Created (4)
- `public` (Application REST Schema)
- `private` (Internal Server Helper Schema)
- `migration` (Raw XT-POS Staging Schema)
- `audit` (Append-only Security Audit Trail)

### Tables Deployed (7)
- `public.system_settings` (RLS Enabled)
- `public.companies` (RLS Enabled)
- `public.branches` (RLS Enabled)
- `public.warehouses` (RLS Enabled)
- `public.company_settings` (RLS Enabled)
- `public.fiscal_periods` (RLS Enabled)
- `public.document_sequences` (RLS Enabled)

### Row Level Security & Access Policies
- RLS enabled on **100% of tables**.
- Explicit SELECT/INSERT/UPDATE grants assigned to `authenticated` role.
- ALL grants assigned to `service_role`.

### Initial Seed Verification (Pre-Live Mode)
- `system_settings`: `SYSTEM_MODE = 'MIGRATION'`
- `companies`: `Casa de Pneus, Lda.` (`a0000000-0000-0000-0000-000000000001`, NUIT: `400123456`)
- `branches`: `Sede Maputo` (`SED`, `b0000000-0000-0000-0000-000000000001`)
- `warehouses`: `Armazém Principal` (`ARM01`, `c0000000-0000-0000-0000-000000000001`, `is_default: true`)
- `fiscal_periods`: 2026 Fiscal Period (`f2026000-0000-0000-0000-000000002026`, status: `open`)
- `document_sequences`: 2026 Series A counters initialized for `FT`, `VD`, `GR`, `NC`, `ND`, `REC`.

---

## 3. Verification Evidence

```json
{
  "system_settings": {
    "status": 200,
    "row": {
      "setting_key": "SYSTEM_MODE",
      "setting_value": "MIGRATION",
      "description": "Current operational mode: MIGRATION, PILOT, LIVE, MAINTENANCE"
    }
  },
  "companies": {
    "status": 200,
    "row": {
      "id": "a0000000-0000-0000-0000-000000000001",
      "name": "Casa de Pneus, Lda.",
      "tax_number": "400123456"
    }
  },
  "warehouses": {
    "status": 200,
    "row": {
      "id": "c0000000-0000-0000-0000-000000000001",
      "name": "Armazém Principal",
      "code": "ARM01"
    }
  }
}
```

---

## 4. Final Deployment Decision

> **STATUS: APPROVED & DEPLOYED TO PRODUCTION**
> The core schema, security policies, and baseline reference seed have been successfully deployed and verified on `bkbcgndzsfylwsinxwbb.supabase.co`.
