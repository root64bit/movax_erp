# Administrator access test results

Administrator login, active profile, branch/warehouse scope, forced password change, and `ADMINISTRATOR` role passed. The protected `admin_update_user_profile` RPC succeeded for an idempotent self-profile verification and wrote an audit event. Effective permissions total 107 while migration execution and system-mode change remain deliberately excluded.
