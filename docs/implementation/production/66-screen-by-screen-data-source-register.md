# Screen-by-screen data source register

The detailed register is [screen-data-source-register.csv](screen-data-source-register.csv). All operational screens use authenticated Supabase data or protected RPCs. The application has one bounded loader for shared operational context and a separate server-paginated report loader.
