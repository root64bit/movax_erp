# Deployment Report: DEPLOYMENT-2026-07-28-03

> **Deployment ID:** DEPLOYMENT-2026-07-28-03  
> **Timestamp:** 2026-07-28 14:51:19 UTC  
> **Target Project Ref:** `bkbcgndzsfylwsinxwbb`  
> **Target Host:** `aws-0-eu-west-1.pooler.supabase.com:6543`  
> **Executor:** Senior Implementation Architect  
> **Pre-Deployment Backup:** Baseline Snapshot `05-pre-deployment-backup-report.md`  

---

## 1. Migrations Applied

| Migration File | Hash / Status | Execution Time | Result |
|----------------|---------------|----------------|--------|
| `20260728180000_003_articles_and_reference_data.sql` | Applied | < 1.4s | **SUCCESS** |

---

## 2. Objects Deployed to Production (`bkbcgndzsfylwsinxwbb`)

### Tables Deployed (7)
- `public.product_families` (RLS Enabled)
- `public.product_categories` (RLS Enabled)
- `public.brands` (RLS Enabled)
- `public.units_of_measure` (RLS Enabled)
- `public.tax_codes` (RLS Enabled)
- `public.products` (RLS Enabled - Cost Price Masking Policy Active)
- `public.price_history` (RLS Enabled - Restricted to `products.view_cost`)

### RLS & Cost-Price Security Policies
- RLS enabled on **100% of tables**.
- Cost-price visibility (`avg_cost`, `last_purchase_cost`, `profit_pct`) and price history logs restricted exclusively to roles with `products.view_cost` permission.
- Non-authorized roles (e.g., `SALES_OP`, `CASHIER`) receive sale prices without cost margins.

### Reference Master Data Seeded
- **Product Families (5):** `PNEU` (Pneus), `JANT` (Jantes), `CAMAR` (Câmaras de Ar), `SERV` (Serviços), `ACES` (Acessórios)
- **Tax Codes (2):** `IVA16` (Mozambique IVA 16% standard rate), `ISENTO` (0% exempt)
- **Units of Measure (5):** `UN` (Unidade), `PR` (Par), `JG` (Jogo), `KG` (Kilograma), `LT` (Litro)
- **Tire & Service Brands (9):** `Bridgestone`, `Michelin`, `Goodyear`, `Continental`, `Pirelli`, `Dunlop`, `Hankook`, `Maxxis`, `Serviço Interno`

---

## 3. Verification Evidence

```json
{
  "families": [
    { "code": "ACES", "name": "Acessórios" },
    { "code": "CAMAR", "name": "Câmaras de Ar" },
    { "code": "JANT", "name": "Jantes" },
    { "code": "PNEU", "name": "Pneus" },
    { "code": "SERV", "name": "Serviços" }
  ],
  "tax_codes": [
    { "code": "IVA16", "rate": "16.00" },
    { "code": "ISENTO", "rate": "0.00" }
  ],
  "units_of_measure": ["UN", "PR", "JG", "KG", "LT"],
  "brands_count": 9
}
```

---

## 4. Final Deployment Decision

> **STATUS: APPROVED & DEPLOYED TO PRODUCTION**
> PROD-WP04 Articles & Reference Data deployed and verified on `bkbcgndzsfylwsinxwbb.supabase.co`.
