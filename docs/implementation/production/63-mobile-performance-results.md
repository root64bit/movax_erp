# Mobile performance results

Production build succeeds with 90 transformed modules. Current compressed assets are approximately 132 kB JavaScript and 8 kB CSS. Dashboard totals are server-computed; operational lists are bounded; reports are server-filtered and paginated; production mock data is absent. Route-level code splitting remains an optimisation for a later performance budget, not a correctness blocker.
