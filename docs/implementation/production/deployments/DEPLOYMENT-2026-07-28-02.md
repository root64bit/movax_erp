# Deployment Report: DEPLOYMENT-2026-07-28-02

> **Deployment ID:** DEPLOYMENT-2026-07-28-02  
> **Timestamp:** 2026-07-28 14:38:36 UTC  
> **Target Project Ref:** `bkbcgndzsfylwsinxwbb`  
> **Target Host:** `aws-0-eu-west-1.pooler.supabase.com:6543`  
> **Executor:** Senior Implementation Architect  
> **Pre-Deployment Backup:** Baseline Snapshot `05-pre-deployment-backup-report.md`  

---

## 1. Migrations Applied

| Migration File | Hash / Status | Execution Time | Result |
|----------------|---------------|----------------|--------|
| `20260728170000_002_auth_rbac_and_rls_foundation.sql` | Applied | < 1.5s | **SUCCESS** |

---

## 2. Objects Deployed to Production (`bkbcgndzsfylwsinxwbb`)

### Tables Deployed (8)
- `public.user_profiles` (RLS Enabled)
- `public.roles` (RLS Enabled)
- `public.permissions` (RLS Enabled)
- `public.role_permissions` (RLS Enabled)
- `public.user_roles` (RLS Enabled)
- `public.branch_access` (RLS Enabled)
- `public.warehouse_access` (RLS Enabled)
- `public.login_events` (RLS Enabled)

### Helper Security RPC Functions (5)
- `public.get_user_company_id()` (`SECURITY DEFINER`, `search_path = public, pg_temp`)
- `public.get_user_permissions()` (`SECURITY DEFINER`, `search_path = public, pg_temp`)
- `public.has_permission(required_permission TEXT)` (`SECURITY DEFINER`, `search_path = public, pg_temp`)
- `public.has_branch_access(target_branch_id UUID)` (`SECURITY DEFINER`, `search_path = public, pg_temp`)
- `public.has_warehouse_access(target_warehouse_id UUID)` (`SECURITY DEFINER`, `search_path = public, pg_temp`)

### Database Triggers (1)
- `public.handle_new_user()` (Trigger `on_auth_user_created` on `auth.users` INSERT)

### RBAC Seed Data Deployed
- **8 System Roles:** `ADMIN`, `MANAGER`, `STOCK_OP`, `SALES_OP`, `CASHIER`, `PURCHASING_OP`, `ACCOUNTING_OP`, `READ_ONLY`
- **53 Granular Permissions:** Spanning Catalogue, Inventory, Sales, Customers, Suppliers, Payments, Reports, Admin
- **Role Permission Assignments:**
  - `ADMIN`: 53 permissions (100% coverage)
  - `MANAGER`: 48 permissions (all except core tenant admin)
  - `STOCK_OP`: 8 stock/inventory permissions
  - `SALES_OP`: 7 sales permissions (cost price viewing masked)
  - `CASHIER`: 8 cashier & receipt permissions

---

## 3. Verification Evidence

```json
{
  "roles_count": 8,
  "roles_list": [
    "ACCOUNTING_OP",
    "ADMIN",
    "CASHIER",
    "MANAGER",
    "PURCHASING_OP",
    "READ_ONLY",
    "SALES_OP",
    "STOCK_OP"
  ],
  "permissions_count": 53,
  "admin_permissions_count": 53
}
```

---

## 4. Final Deployment Decision

> **STATUS: APPROVED & DEPLOYED TO PRODUCTION**
> PROD-WP03 Auth, RBAC & RLS foundation deployed and verified on `bkbcgndzsfylwsinxwbb.supabase.co`.
