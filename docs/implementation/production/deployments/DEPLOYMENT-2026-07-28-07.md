# Deployment Report: DEPLOYMENT-2026-07-28-07

> **Deployment ID:** DEPLOYMENT-2026-07-28-07  
> **Timestamp:** 2026-07-28 15:08:53 UTC  
> **Target Project Ref:** `bkbcgndzsfylwsinxwbb`  
> **Target Host:** `aws-0-eu-west-1.pooler.supabase.com:6543`  
> **Executor:** Senior Implementation Architect  
> **Pre-Deployment Backup:** Baseline Snapshot `05-pre-deployment-backup-report.md`  

---

## 1. Migrations Applied

| Migration File | Hash / Status | Execution Time | Result |
|----------------|---------------|----------------|--------|
| `20260728220000_007_sales_and_purchase_documents.sql` | Applied | < 2.1s | **SUCCESS** |

---

## 2. Objects Deployed to Production (`bkbcgndzsfylwsinxwbb`)

### Tables Deployed (9)
- `public.document_types` (RLS Enabled - 10 Document Types Seeded)
- `public.documents` (RLS Enabled - Duplicate Supplier Invoice Unique Index Active)
- `public.document_lines` (RLS Enabled)
- `public.document_transport_details` (RLS Enabled)
- `public.document_links` (RLS Enabled)
- `public.document_status_history` (RLS Enabled)
- `public.ledger_entries` (RLS Enabled - Current Account Financial Foundation)
- `migration.documents_raw` (Service-Role Only)
- `migration.document_lines_raw` (Service-Role Only)

### RPC Procedures Deployed (6)
- `private.next_document_number(...)` (`SECURITY DEFINER`)
- `public.recalculate_document(...)` (`SECURITY DEFINER`)
- `private.confirm_customer_document(...)` (`SECURITY DEFINER`)
- `private.confirm_supplier_document(...)` (`SECURITY DEFINER`)
- `private.reverse_confirmed_document(...)` (`SECURITY DEFINER`)

### Seed Master Data
- **Document Types (10):** `CUSTOMER_DELIVERY_NOTE`, `CUSTOMER_INVOICE`, `CASH_SALE`, `CUSTOMER_CREDIT_NOTE`, `CUSTOMER_DEBIT_NOTE`, `SUPPLIER_DELIVERY_NOTE`, `SUPPLIER_INVOICE`, `SUPPLIER_CREDIT_ADVICE`, `SUPPLIER_DEBIT_ADVICE`, `SUPPLIER_RETURN`
- **Permissions (28):** `documents.*`, `sales.*`, `purchases.*` mapped to roles.

---

## 3. End-to-End Test Suite Evidence

```text
=== TEST RESULTS ===
1. Customer Delivery Note (CUSTOMER_DELIVERY_NOTE A/000001): Confirmed, stock exit posted (300 -> 290) -> PASS
2. Customer Invoice (CUSTOMER_INVOICE A/000001): Confirmed, 29,000.00 MZN grand total, customer ledger entry posted (Debit 29,000.00 MZN), customer balance updated to 29,000.00 MZN -> PASS
3. Duplicate Supplier Invoice Rejection (INV-SUPP-999): Unique constraint uq_active_supplier_invoice_number caught duplicate cleanly -> PASS
4. Document Reversal (reverse_confirmed_document): Reversed document status to REVERSED, logged cancellation reason, reversed stock exit movement, reversed financial ledger entry status to REVERSED -> PASS
```

---

## 4. Final Deployment Decision

> **STATUS: APPROVED & DEPLOYED TO PRODUCTION**
> PROD-WP08 Commercial Document Engine deployed, verified, and tested 100% on `bkbcgndzsfylwsinxwbb.supabase.co`. System mode remains `MIGRATION`.
