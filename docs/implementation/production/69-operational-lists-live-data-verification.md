# Operational lists live-data verification

Products, inventory balances, customers, suppliers, documents, movements, payments, ledger entries, and users are read from production tables under RLS with explicit columns, ordering, and limits. Payment terms, payment methods, product categories, brands, units, branches, and warehouses are dynamically loaded from database reference data.
