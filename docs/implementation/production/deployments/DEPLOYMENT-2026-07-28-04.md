# Deployment Report: DEPLOYMENT-2026-07-28-04

> **Deployment ID:** DEPLOYMENT-2026-07-28-04  
> **Timestamp:** 2026-07-28 14:53:12 UTC  
> **Target Project Ref:** `bkbcgndzsfylwsinxwbb`  
> **Target Host:** `aws-0-eu-west-1.pooler.supabase.com:6543`  
> **Executor:** Senior Implementation Architect  
> **Pre-Deployment Backup:** Baseline Snapshot `05-pre-deployment-backup-report.md`  

---

## 1. Migrations Applied

| Migration File | Hash / Status | Execution Time | Result |
|----------------|---------------|----------------|--------|
| `20260728190000_004_stock_engine.sql` | Applied | < 1.6s | **SUCCESS** |

---

## 2. Objects Deployed to Production (`bkbcgndzsfylwsinxwbb`)

### Tables Deployed (7)
- `public.inventory_balances` (RLS Enabled)
- `public.stock_movement_reasons` (RLS Enabled)
- `public.stock_movements` (RLS Enabled - Immutable Movement Ledger)
- `public.inventory_counts` (RLS Enabled)
- `public.inventory_count_lines` (RLS Enabled)
- `public.stock_transfers` (RLS Enabled)
- `public.stock_transfer_lines` (RLS Enabled)

### Transactional Stock RPC Functions (1)
- `public.post_stock_movement(...)` (`SECURITY DEFINER`, `search_path = public, pg_temp`)
  - Calculates real-time stock balance & weighted average cost (MAC).
  - Enforces negative stock guardrails based on company settings & permissions.
  - Inserts immutable `stock_movements` ledger entry.
  - Updates `inventory_balances` and `products.avg_cost` atomically.

### Stock Movement Reasons Seeded (6)
- `ENT_DIR`: Entrada Directa Manual
- `SAI_DIR`: Saída Directa Manual
- `AJUSTE_POS`: Ajuste Positivo de Inventário (Contagem)
- `AJUSTE_NEG`: Ajuste Negativo de Inventário (Contagem)
- `DETERIORA`: Artigo Danificado / Deteriorado
- `OFERTA`: Oferta / Amostra Comercial

---

## 3. Verification Evidence

```json
{
  "stock_movement_reasons": [
    { "code": "ENT_DIR", "description": "Entrada Directa Manual" },
    { "code": "SAI_DIR", "description": "Saída Directa Manual" },
    { "code": "AJUSTE_POS", "description": "Ajuste Positivo de Inventário (Contagem)" },
    { "code": "AJUSTE_NEG", "description": "Ajuste Negativo de Inventário (Contagem)" },
    { "code": "DETERIORA", "description": "Artigo Danificado / Deteriorado" },
    { "code": "OFERTA", "description": "Oferta / Amostra Comercial" }
  ],
  "post_stock_movement_rpc_verified": true
}
```

---

## 4. Final Deployment Decision

> **STATUS: APPROVED & DEPLOYED TO PRODUCTION**
> PROD-WP05 Stock Engine & Balance Posting deployed and verified on `bkbcgndzsfylwsinxwbb.supabase.co`.
